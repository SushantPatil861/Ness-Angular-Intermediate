import { ApplyShadowDirective } from './apply-shadow.directive';

describe('ApplyShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyShadowDirective();
    expect(directive).toBeTruthy();
  });
});
